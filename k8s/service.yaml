apiVersion: v1
kind: Service
metadata:
  name: gitops-demo-api
  namespace: default
spec:
  type: ClusterIP

  selector:
    app: gitops-demo-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000